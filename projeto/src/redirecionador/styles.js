import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  textinho:{
    fontSize: 23,
    textAlign: 'center',
    justifyContent: 'center',
    margin: 8,
    color: '#0014A8',
    margin: 20
  }
})

export {styles}